{
  "name": "typed_i18n_sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "../index.js -i ./src/locale.json -o ./src -p ja -l flow -l typescript",
    "show-version": "../index.js --version",
    "test": "yarn show-version && yarn build && flow && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "flow-bin": "^0.80.0",
    "typescript": "^2.7.0-dev.20171027"
  }
}
